{
  "name": "ai-chan-slack-bot-app",
  "version": "2.0.0",
  "description": "AI-chan",
  "main": "app.js",
  "scripts": {
    "start": "npm run start-test & npm run start-nightcord",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-test": "node --env-file=.env-test app.js",
    "start-nightcord": "node --env-file=.env app.js"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^4.2.0"
  }
}
